specVersion: "v1"
serviceId: "siterpg"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "siterpg-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
